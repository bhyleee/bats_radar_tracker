name: bats_env
channels:
  - conda-forge
dependencies:
  - tensorflow=2.11.0  # Specify a compatible version of TensorFlow
  - keras=2.11.0
  - jupyterlab
  - arm_pyart
  - gdal
  - rasterio
  - geopandas
  - seaborn
  - r-irkernel
  - dask
  - coverage
  - pytest
  - mamba
  - pip
  - pip:
    - nexradaws
prefix: /Users/brianlee/miniforge3/envs/doppler_tf
